(define init
  (lambda ()
    (display "hello from player init")))

(define update
  (lambda ()
    (display "hello from player update")
    ))

(define collision
  (lambda ()
    (display "collision from guile!")))
