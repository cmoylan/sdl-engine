#include "Entity.h"

// TODO: fill in later
AssetList Entity::assetData()
{
    AssetList data;
    return data;
}


void Entity::handleCollisionEvent(Message message)
{
    // if this is the exit object
    // and we collide with the player
    // broadcast end message
}
